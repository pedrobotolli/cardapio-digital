import { createTheme } from '@mui/material/styles';

export const mcTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#9A0404"
        },
        secondary: {
            main: "#FFC31D"
        }
    }
})
